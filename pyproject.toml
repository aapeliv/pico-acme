[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pico_acme"
dynamic = ["version"]
description = "Pico ACME: tiny ACMEv2 client"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
authors = [{ name = "Aapeli Vuorinen" }]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = [
    "acme==5.3.1",
    "cryptography>=43.0.0",
    "PyOpenSSL>=25.0.0",
    "pyrfc3339",
]

[project.optional-dependencies]
route53 = ["boto3"]

[project.urls]
GitHub = "https://github.com/aapeliv/pico-acme"
"Bug Tracker" = "https://github.com/aapeliv/pico-acme/issues"

[dependency-groups]
dev = ["black", "build", "isort", "pytest", "twine"]

[tool.setuptools.dynamic]
version = { file = "src/pico_acme/version" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
pico_acme = ["version"]

[tool.black]
line-length = 120

[tool.pytest.ini_options]
markers = [
    "integration: mark test as integration test (needs AWS creds + network)",
]

[tool.isort]
skip_gitignore = true
include_trailing_comma = true
multi_line_output = 3
line_length = 120
